enum ProjectStatus { planned, active, paused, completed, cancelled }

enum DonorType { individual, institutional }

enum BudgetCategory { transport, food, logistics, training, health, education, equipment, staff, utilities, other }
